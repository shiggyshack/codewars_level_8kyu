//Create a function, that accepts two parameters: one a number, one a string, and return true if they are the same, just not the same data type; otherwise, return false.
function add(a, b){
  //The '==' will be used to determine if the implicit values of the varibles are equal without having to convert data types.
  if(a == b){
    return true
  }
  //Return 'false' if this comparision doesn't hold true.
  else{
    return false
  }
}
