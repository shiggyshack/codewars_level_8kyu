// DESCRIPTION:

// Write a function which converts the input string to uppercase.

function makeUpperCase(str) {
    //create new string to hold the upper case version of str
    let nowUpper = str.toUpperCase()
    return nowUpper
  }