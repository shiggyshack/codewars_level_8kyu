//Write a quote function that takes a parameter of a fighter's name and returns a string depending on the fighter whose name is entered.
var quote = function(fighter) {
  //Use toLowerCase() so the string will not be case sensitive.
  if(fighter.toLowerCase() === 'george saint pierre'){
    return 'I am not impressed by your performance.'   
  }
  //Use toLowerCase() so the string will not be case sensitive.
  if(fighter.toLowerCase()=== 'conor mcgregor'){
    return "I'd like to take this chance to apologize.. To absolutely NOBODY!"
  }
};
