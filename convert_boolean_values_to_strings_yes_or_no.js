// DESCRIPTION:

// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

//One function using a if/else conditional structure to determine between two stings to return.

function boolToWord( bool ){
    if(bool){
      return "Yes"
    }
    else{
      return "No"
    }
  }